# 发布包与本地数据安全

发布包只应包含源码、文档、经过路径裁剪的程序和图标，不包含预置账号或密钥。第一次启动会在本机生成数据。

禁止上传或分享：

- `data/`、`auths/`、`logs/`、`backups/`、浏览器/WebView2 配置。
- `master.key`、SQLite 数据库、DPAPI 凭据、真实 `.env`、账号令牌、API Key。
- 带真实授权参数的回调 URL、截图、剪贴板内容和诊断日志。

`deploy/.env.example` 是空凭据模板，可以发布。协议公钥与合成测试数据可以保留。

工作台仅监听本机回环地址，不适合暴露到公网。运行日志可能包含账号标识，不随发布包上传。请使用 `tools/package.ps1` 的源码白名单导出与全新构建流程，不要直接压缩正在使用的安装目录。

发布程序未做商业代码签名；请核对随包提供的 SHA-256 摘要。不要在公开 issue 中提交真实密钥或账户数据。
